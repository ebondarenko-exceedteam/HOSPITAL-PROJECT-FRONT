import React from "react";
import Header from "../Header/Header";

const RegistrationPage = ({headerTitle}) => {
  return (
    <Header headerTitle={headerTitle} />
  )
}

export default RegistrationPage;